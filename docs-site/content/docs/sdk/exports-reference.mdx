---
title: Exports reference
description: Every public export from @agenttrust-sdk/trustgate, grouped by import path.
---

The SDK exposes three import surfaces: the Express middleware, the client helpers, and the root namespace (atomicity guard, PDA derivers, shared constants).

(Populated in C6 — placeholder during scaffold.)

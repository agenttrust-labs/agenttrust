$ cat mcp-config-mainnet.json
{
  "mcpServers": {
    "agenttrust": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@agenttrust-sdk/mcp@latest"],
      "env": {
        "RPC_URL": "https://api.mainnet-beta.solana.com",
        "NETWORK": "solana-mainnet"
      }
    }
  }
}
# Identical to previous gate's config - no AT program ID workarounds.

--- 1. mainnet boot probe (direct stdio) ---

$ NETWORK=solana-mainnet RPC_URL=https://api.mainnet-beta.solana.com node probe-mcp.js
{
  "initialize": {
    "result": {
      "protocolVersion": "2024-11-05",
      "capabilities": { "tools": {}, "resources": {}, "prompts": {} },
      "serverInfo": { "name": "agenttrust", "version": "0.3.4" },
      "instructions": "AgentTrust MCP server. ..."
    },
    "id": 1
  },
  "toolsList": { "result": { "tools": [ ... 19 entries ... ] }, "id": 2 },
  "stderr":
    "[agenttrust] WARN mainnet selected but AgentTrust programs are not deployed to mainnet yet.
     Read tools that touch policy_vault / trustgate / validation_registry will fail with
     errorCode: \"config_error\". Quantu reads (get_quantu_reputation, agent_account lookups)
     are unaffected. To override, set POLICY_VAULT_PROGRAM_ID / TRUSTGATE_PROGRAM_ID /
     VALIDATION_REGISTRY_PROGRAM_ID."
}
# WAS in 0.3.3:
#   fatal: Error: AgentTrust programs are not yet deployed on mainnet.
#   at loadConfig (.../dist/config.js:202:15)
#   [MCP] subprocess exited before initialize completed
# NOW in 0.3.4:
#   warn at startup, all 19 tools available, server runs.


--- 2. Quantu mainnet read (works out-of-the-box, no workaround needed) ---

$ claude -p "Call agenttrust_get_quantu_reputation with agent_asset FHvoK58UogWy9ZrX7tHGLcRYUD7qfQZjHtDcMoxZShMc. Report the response." \
    --strict-mcp-config --mcp-config mcp-config-mainnet.json \
    --permission-mode bypassPermissions \
    --allowedTools 'mcp__agenttrust__*' \
    --output-format json --model sonnet

[MCP] starting stdio server: npx -y @agenttrust-sdk/mcp@latest
[MCP] subprocess wrote to stderr: [agenttrust] WARN ...   (warn, not throw)
[MCP] connected: agenttrust v0.3.4
[MCP] tools/list returned 19 tools

[Claude] (turn 1) Calling agenttrust_get_quantu_reputation.

[Tool output]
{
  "pda":           "7g7Ah5vkXWs9TJKvGApKJyEUnvCPJsvA4mePDfKUVMXK",
  "explorerUrl":   "https://explorer.solana.com/address/7g7Ah5vkXWs9TJKvGApKJyEUnvCPJsvA4mePDfKUVMXK",
  "exists":        false,
  "ownerProgram":  null,
  "ownerExpected": "AToMw53aiPQ8j7iHVb4fGt6nzUNxUhcPc3tbPBZuzVVb",
  "ownerMatches":  false,
  "rawByteLen":    0
}

# Quantu mainnet read path WORKS without any AgentTrust program ID workaround.
# The atom_engine program ID resolved correctly on mainnet (matches mainnet-quantu-tier0-scan.json).
# exists:false is the sentinel quirk (the test pubkey is itself an atom_stats PDA, not a raw agent_asset).

[session ended - exit 0, duration 12s]


--- 3. AT-touching tool on mainnet returns structured config_error envelope ---

$ claude -p "Call agenttrust_get_policy with agent_asset FHvoK58UogWy9ZrX7tHGLcRYUD7qfQZjHtDcMoxZShMc and policy_id 1. Report the full raw response verbatim." \
    --strict-mcp-config --mcp-config mcp-config-mainnet.json \
    --permission-mode bypassPermissions \
    --allowedTools 'mcp__agenttrust__*' \
    --output-format json --model sonnet

[Tool output]
{
  "errorCode": "config_error",                                  <-- new error code class
  "message":   "MCP server is misconfigured for this tool.",
  "hint":      "AgentTrust policy_vault program is not deployed on mainnet yet.
                Set POLICY_VAULT_PROGRAM_ID to an explicit base58 program ID,
                or use NETWORK=solana-devnet where AgentTrust programs are already deployed.
                Quantu reads (get_quantu_reputation, agent_account lookups) are
                unaffected and work on mainnet without any AgentTrust program ID override.",
  "cause":     "AgentTrust policy_vault program is not deployed on mainnet yet. ..."
}

# Structured envelope with the new config_error code, exactly as the brief specified.
# Caller knows exactly what to do (set the env var or switch to devnet).

[session ended - exit 0, duration 10s]

{
  "name": "agenttrust-attestor-demo",
  "version": "0.1.0",
  "private": true,
  "description": "AgentTrust ValidationRegistry — devnet attestor lifecycle demo",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "smoke": "ts-node scripts/devnet-attestor-flow.ts",
    "test": "ts-mocha -p ./tsconfig.json -t 30000 \"test/**/*.test.ts\"",
    "test:integration": "INTEGRATION=1 ts-mocha -p ./tsconfig.json -t 120000 \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@agenttrust-sdk/trustgate": "workspace:*",
    "@coral-xyz/anchor": "^0.31.1",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "@types/node": "^20.0.0",
    "bs58": "^6.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^4.9.5"
  }
}
